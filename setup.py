from setuptools import setup

setup(
    name='database',
    version='0.1',
    description='wrapper for sqlite database module',
    author='Darian Patzak',
    packages=['database'],
    zip_safe=False
)
